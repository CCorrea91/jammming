let accessToken;

class Spotify extends React.Component {

    getAccessToken() {
        if (accessToken) {
            return accessToken
        } else {
            
        }
    }
};

export default Spotify;
